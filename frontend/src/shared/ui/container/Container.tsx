// Обертка для контента страницы: задает max-width, центрирует и добавляет стандартные боковые отступы,
// чтобы интерфейс выглядел единообразно на разных экранах.
// Можно использовать в header/footer, на страницах и внутри full-width секций (фон на всю ширину + контент в Container).
